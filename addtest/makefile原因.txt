1、工程中的源文件很多，手动编译每个源文件、链接目标文件会
变得繁琐，Makefile可以设置想要的编译规则，编写makefile，使
用make命令来构建工程，会使工作简化，提高效率。
2、当编译整个项目工程的时候，make只会编译我们修改过的文件，
没有修改过的不再用重新编译，因此再修改小的debug后重新编译就
不用花费大量的编译时间。
3、良好的Makefile可以清晰展示项目的构建结构和依赖关系，便于
理解项目的组织方式，随着项目复杂度的增加，可以轻松添加新的编译
规则，使构建过程更加灵活。